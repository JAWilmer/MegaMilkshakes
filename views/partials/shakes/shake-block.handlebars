<li>
	{{name}}

	<button class="btn" id="change-consumed" data-id="{{id}}" data-newconsumed="{{consumed}}">
		{{#if consumed}} Move{{else}} Move{{/if}}
	</button>

	<button class="btn" id="delete-shake" data-id="{{this.id}}"> Delete</button>
</li>